
	非重复的字符串，按题目要求需要连接在一起有效。
	想法1：从字符串开头遍历每个字符串，若下一个字符重复则停止，计算当前字符串长度并作为该字符串开头索引的值。
	
	答案：依然做字典键值对，将用过的字符形成新的字典，如果新字符用过，则在上一次该字符位置处+1作为新的其实点（如果上一次该字符在start之前或等于start则忽略）
		  如果没用过，则计算当前位置距离start处距离+1，最后将当前位置字符划入用过字符集。